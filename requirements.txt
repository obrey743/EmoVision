# CPU-friendly defaults; pin numpy for OpenCV compatibility.
# If on Apple Silicon, consider: pip install tensorflow-macos tensorflow-metal (and remove plain tensorflow).
numpy==1.26.4
opencv-python==4.10.0.84
tensorflow==2.17.0
matplotlib>=3.8.0
scikit-learn>=1.4.0
